# ReAcTree implementation for WebShop environment

## Run

```bash
python -m webshop_solution.run_webshop_reactree
```

The script writes a JSON trajectory log to `webshop_trajectories/`.

<!-- llama-server -m /Users/vikas/Downloads/llama3-local/model.gguf -->